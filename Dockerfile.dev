# LUNAR frontend — local dev (Vite HMR + volume mount)
# Build context: frontend/ — source mounted at runtime via compose

FROM node:alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
