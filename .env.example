# Copy to .env.local — Next.js โหลดอัตโนมัติ
# cp .env.example .env.local

# Browser เรียก API ที่ host (port ที่ compose publish)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API_INTERNAL_URL ตั้งใน compose.dev.yaml ตอนรัน docker compose — ไม่ต้องใส่ที่นี่
